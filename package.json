{
  "name": "safelight-batch-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --format=esm --jsx-factory=h --jsx-fragment=React.Fragment --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
